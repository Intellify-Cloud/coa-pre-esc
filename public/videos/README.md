Place your uploaded video files here. Recommended filename(s):

- `beach.mp4` — the drone footage for the hero background (used by the default hero).
- `beach.jpg` — optional fallback image for reduced-motion or older browsers.

Usage notes:
- The hero component references `/videos/beach.mp4` as the default source.
- For best results use an MP4 (H.264) encoded file, optimized for web and sized appropriately.
- Keep the filename lowercase and replace the sample names above with your file.

After placing the files, reload the dev server or rebuild so Vite serves the new static files.
